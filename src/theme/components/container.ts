import { ComponentStyleConfig } from "@chakra-ui/react";

export const container: ComponentStyleConfig = {
    baseStyle: {
        maxWidth: '100%',
        width: 'container.xl',
        px: '16px',
    }
}